grpcio
grpcio-tools
protobuf
pydantic-settings
PyYAML>=5.4 # Added for centralized YAML configuration
python-json-logger>=2.0 # For JSON logging
grpcio-health-checking>=1.48.1 # For gRPC health checks (compatible with recent grpcio)
pybreaker>=1.0.0 # For circuit breaker pattern
grpcio[ares]>=1.48.1 # To ensure 'ares' DNS resolver is available for gRPC client-side LB
asyncpg # For GLM PostgreSQL external connector (dev/integration)
qdrant-client>=1.7.3
aiosqlite>=0.19.0
# TODO: Re-enable sentence-transformers when disk space issue is resolved
# sentence-transformers>=2.2.2
cachetools>=5.3.1
